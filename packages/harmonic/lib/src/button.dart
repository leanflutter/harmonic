import 'package:flutter/widgets.dart';

class HarmonicButton extends StatelessWidget {
  const HarmonicButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
